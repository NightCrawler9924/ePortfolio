# deepanshsabharwal.com

Personal site. One hand-written `index.html`, no build step, no framework. Deployed by GitHub Pages from `main`.

- `index.html`: the whole site, CSS and inline SVG diagrams included
- `Deepansh-Sabharwal-Resume.pdf`: current resume
- `CNAME`: custom domain

To update: edit `index.html`, replace the PDF if needed, push to `main`.
